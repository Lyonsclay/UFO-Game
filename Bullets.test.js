import expect from 'expect';
import React from 'react';
import { render } from 'react-dom';
import Bullets from 'Bullets';

describe('<Bullets> initial state.', () => {
  let node;
  beforeEach(() => {
    node = document.createElement('div');
  });
});
